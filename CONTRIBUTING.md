# Contribuindo

1. Crie uma branch a partir de `main`.
2. Execute `npm install`.
3. Marque narrativas novas como fictícias.
4. Não adicione vazamentos, recursos não licenciados ou segredos.
5. Execute `npm run lint` e `npm run build` antes do pull request.
